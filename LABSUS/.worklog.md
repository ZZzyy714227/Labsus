# LABSUS · .worklog.md

> 协议：先登记后动手；改前查占用；一次一个领域；收尾必归档；测试是完成的一部分。
> 坐标系：v3 主线 X 外/Y 前/Z 上（V1 冻结线 X 前/Y 右）。共享文件红线：`balance` 类不存在，本项目共享敏感文件为 `chassis.py` / `events` 无 —— 以占用表为准。

## 一、元信息

- 项目：LABSUS FSAE 悬架分析工作台
- 测试基线：pytest **102 passed**（2026-08-30 第四波实测，100 基线 + 2 新增 fullchassis与Autocross对拍；系统 Python `C:\Users\zzy\AppData\Local\Programs\Python\Python313\python.exe`）
- 前端防线：`node test_dom.js` **71 项断言全过**（含 fullchassis/allinone 双端 TPHYS 闭包沙箱求值、GT3 赛道/日常双模与 Baja 越野大行程预设校验）

## 二、任务看板

| ID | 任务 | 状态 | 占用会话 | 备注 |
|---|---|---|---|---|
| G1 | grill-me 修复波次 W1+W2+W3 | ✅ 完成（R-0830-1 + R-0830-2） | WorkBuddy + 川陀会话 | 报告：`C:\Users\zzy\Desktop\New_suspension\grill-me\LABSUS_grill_me_全功能审查报告.md` |
| G2 | W4 口径统一 | ✅ 完成（R-0830-3）——故意不同清单 + 一致性对拍 | 川陀会话 | 产物：`INTENTIONAL_DIFFERENCES.md` + `tests/test_w4_consistency.py` |
| G3 | W5 体验与偿债 | ✅ 完成（R-0830-3 主体；F-48/F-50 等纯 docs 级登记） | 川陀会话 | 死代码清理 + 前端体验修复 |
| G4 | 修后更新讲义（learning/explainers 勘误波次） | ✅ 完成（R-0830-4，2026-08-30 十一讲全链审查 + 约 70 处修正） | 川陀会话 | 记录：`learning/learning-records/0006-lectures-synced.md` |
| G5 | 全链手册合订本（记忆主干+连接地图） | ✅ 完成（R-0830-5，2026-08-30；`learning/全链手册.md` + PDF 10 页） | 川陀会话 | 记录：`learning/learning-records/0007-全链手册.md` |
| G6 | 赛道功能演进 M1（双端自洽与 FSAE 官方赛道库） | ✅ 完成（R-0830-6，2026-08-30） | 川陀会话 | fullchassis 注入 TPHYS 离线闭包 + 官方四大赛道预设 + 对拍测试扩充 |
| G7 | 赛道脱轨保险、驾驶系统升级与非交叉拓扑重构 | ✅ 完成（R-0830-7，2026-08-30） | 川陀会话 | 自动回溯前置安全点重置 + 全收敛前瞻制动规划 + 零自交上赛拓扑 |
| G8 | 历史悬架分配（GT3 赛道/日常双模自由切换 + Baja 越野大行程） | ✅ 完成（R-0830-8，2026-08-30） | 川陀会话 | 提取并注入 RACE/SPORT/SUV 三套经典悬架，UI 顶栏/侧栏一键双模切换 |

## 三、文件占用登记

（G1–G3 全部完成，占用清空；遗留登记见第四节）

## 四、归档区

### R-0830-1（第一波，WorkBuddy）
引擎 F-09/F-15/F-16/F-22/F-23(误读澄清)/F-24/F-25/F-63 落码 + test_grill_fixes.py（7 项物理断言）+ requirements.txt（F-73）+ 前端 F-01/F-11/F-51/F-52 卫哨与 test_dom.js 重写（F-59）。

### R-0830-2（第二波，川陀会话）
G1 遗留 6 个失败测试全绿（86 passed）：
1. `test_slip_relaxation_lags_kinematic`：改稳态段中位数断言（滞后在目标下降段瞬时高于当前目标是正确物理）。
2. `test_tlltd_negative_g_not_pinned_at_50`：断言语义修正——镜像对称应 pos≈neg（实现正确，测试期望错）。
3. `test_quasi_loads_zero_rc_height_not_swallowed` / `..._roll_instability_reports_warning`：rc_sw 空 dict 违反契约 → 合法最小 sweep + 失稳用例显式 arb.d=0。
4. **对拍基建打通（F-58）**：tphys_parity.cjs（ctx 优先 / JS 结果落盘 argv[3] / pyOut 走 tmpdir）+ tphys_ref.py 双路径注入。
5. `test_dom.js` F-11 断言剥离注释后匹配 + isFinite 卫哨断言。
验证：86 passed + 25 项 DOM。

### R-0830-3（第三波，川陀会话）——本次全部"剩下"修复项
**引擎侧（G2/W4 + 补偿）**：
- F-65：`bush_force_jac` 解析雅可比接进 `solve_compliance` 外层 TRF（块对角 −J_bush；全套 22.5s→13s 提速实证）。
- F-64：cp_rel 默认一致化（tire_radius=325，vs 旧 320 矛盾）+ 力矩折算进轮心 + 超静定/力矩残余降级 APPROXIMATE（球铰模型无法平衡力矩，显式上报）。
- F-66：bushing table/spline 越界不再 clip（端点刚度线性外推，物理硬化持续）+ 非单调 x 显式校验。
- F-67：`mr_at_zero` 不再吞异常（rep.ok 检查 + 抛出让 `_resolve_mr_fb` 统一兜底）；回退常量分轴（后轴 0.78 修正）。
- F-68：`_apply` 视图别名 → copy。
- F-70：`axle_rc_sweep` 缓存加锁（线程安全）。
- F-71：solve_rocker 二分缓存 err(lo)；steer_axis 默认值 (−1,0,0)（A.3 残留）。
- F-45：pydantic 校验缺口（硬点 3 分量+有限性、SweepSpec min≤max+有限、TireParams FzNom/By>0、TrackPoint target_speed≥0、ChassisRequest axle points）——新增 `tests/test_f45_validation.py`（7 项）。
- F-46：server.py 异常 detail 脱敏（traceback 进日志，响应只留类型）。
- F-49：run_pose 冗余 `make_bushings([], points)` 已无（复核）。
- F-12/F-13：`_slope` 三处拷贝收敛到 `metrics/kandc.py` 唯一内核（最近邻中央差分 + None 跳过 + 端点单侧）；kinematics 状态机版委托同源（双语义收敛）。
- F-26：transient 动力半径按轴 tire_radius 折算（旧硬编码 0.30）。
- F-19：transient MR 静态回退分轴持 0.75/0.78 并登记故意不同清单。
- 死代码：`solver/mechanism/project.py`（PBD 投影原语零消费者）删除。
- 新增 `tests/test_w4_consistency.py`（7 项）：v3 设计位自洽锚（cam/toe 精确回读）+ V1 冻结线保留登记 + 差分内核单一性 + MR 优先级/分轴回退。

**前端侧（G3/W5）**：
- F-44：E() innerHTML→textContent（含 sec() 标题节点化，消除引擎响应 XSS 面）。
- F-53：UI.sync 重建截断（syncBase 记录，buildRight 重建前回卷，防只增不减）。
- F-54：allinone 预瞄增益死控件 → TRK.lookahead 真状态。
- F-47：前端扫掠 min/max 交互纠偏（避免 min>max payload）。
- F-04：摇臂双根分支连续性（lastTheta 最近根选择，fullchassis+allinone）。
- F-05：findLimits NaN 中断扫描（isFinite 判定）。
- F-14：ackermann `(L?2:2)` 笔误 → `*2`。
- F-17：报告簧载质量按前后轴 mS 比例分配（旧 45/55 硬编码）。
- F-18：dimScores 复用 EVAL_BENCHMARKS eval() 评级（good90/warn62/bad30，双轨矛盾消除）。
- F-20：暂停真正冻结时域积分（simulate(S.play?dt:0)，两文件）。
- F-21：stepDyn 子步轻量接地点 z（cosCam 投影，与 metrics.cp[2] 数学一致）。
- F-28：TPHYS makeSimContext 注入 ARB 等效刚度（arbK 与 arbRate(0.55) 逐行同构）。
- F-30：15-DOF 制动阻力点头符号 + 后轴侧倾力臂 tR。
- F-32：15-DOF 读 SIM_data.mrRefF/R 真实 MR。
- F-33：circuitStage 监听器防泄漏（_circuitDragHandlers 注册/移除）+ dt 走真实帧间隔。
- F-34：circuit 渲染补 p/n 图元（slope/skidpad 一致）。
- F-35：slope/skidpad 轮胎转角改真积分（lastDt 帧累计）。
- F-36：hpImport 恢复 tieTrimF/R。
- F-37：loadVehiclePreset 清 baseline/ENG/CH 结果。
- F-38：allinone 赛道 JSON/CSV 导入字段与有限性校验。
- F-42：IC 十字线竖线端点修正。
- F-43：死开关替换为真实消费键（halfshaft/disc_vent/steer_rack）。
- F-55：空格键仅在 TRK 回放激活态生效（slope/skidpad 手刹恢复）。
- F-56：`.sh.blu` CSS 补定义。
- F-57：allinone STAGE/trackStep 回放倍率改时间累积（不再 round 漂移）。
- F-39：deepClone 双轨收敛（loadVehiclePreset/initData）。
- F-41：circPts/cylinder/springPts 重复定义清理（保留 fillCol 超集）。
- F-40：buildScenePRO 复用 SIM.m* 指标（每帧不再 4 次重算）。
- **F-03**：拖拽改轻量路径——mousemove 中只 buildMech+driveTo+metrics 预览（保留当前轮跳），pointerup 一次性 rebuild() 全量刷新（旧每像素全量 rebuild 含 findLimits+runSweep，必然卡顿）。
- `test_dom.js` 扩到 **57 项断言**（含 R-0830-3 全部修复的回访断言）。

**验证**：`pytest 100 passed`（12.8s；93 基线 + 7 新增 W4/F45）+ `node test_dom.js` 57 项全过。

**W4 产物**：`INTENTIONAL_DIFFERENCES.md`（坐标系双血脉/定位角双权威/差分窗口/MR 三路/ARB 三处/transient 半径 的登记真源 + 对拍钉死的禁止漂移清单）。

### R-0830-6（第四波，M1 赛道仿真双端自洽与 FSAE 官方赛道库）
- **前端工作台**：
  - `dwb-pro-fullchassis.html` 成功注入 `TPHYS` 离线物理闭包（3-DOF + MF 复合滑移 + 松弛长度 + 准静态载荷转移 + 纯追踪/PI 控制器），彻底消除未连接 Python 引擎时的功能阻断。
  - `TRACK_PRESETS` 扩充 FSAE 官方标准赛道：`autocross`（800m 综合单圈）、`accel`（75m 直线加速与制动区）、`skidpad8`（8 字双定圆）。
  - `fullchassis` 与 `allinone` 双端赛道侧栏 `trackRun` 与独立 3D 舞台 `trackStageRun` 均升级为无缝双模（在线走 Python `/api/v3`，离线自动调用 `TPHYS.run` 并输出 `[内置JS]` 状态）。
- **测试与对拍防线**：
  - `node test_dom.js` 扩展至 **67 项全真断言**（新增 `fullchassis` TPHYS 闭包沙箱提取与求值、FSAE 官方赛道预设字段检查全部通过）。
  - `test_tphys_parity.py` 扩充 2 项新测试（`test_tphys_parity_fullchassis_skidpad` 与 `test_tphys_parity_fsae_autocross`），pytest 测试集扩充至 **102 passed**，双端数值相对误差严格控制在 5% 容差内。

**验证**：`pytest 102 passed`（16.1s）+ `node test_dom.js` 67 项全过。

### R-0830-7（第五波，赛道脱轨保险机制、自动驾驶算法升级与非交叉拓扑重构）
- **脱轨保险机制（Auto-Respawn Insurance）**：
  - 彻底修复 `CircuitPath.getLookahead` 缺失 `nx/ny` 法向矢量引发 `NaN` 扩散导致黑屏的致命 Bug。
  - 在 `circuitStageLoop` 中引入自动脱轨保护（$|e_y| > 5.8\text{m}$ 或状态非有限值时自动触发）：回溯至前序 40 米（22 个路点前）的安全点，赋平稳起步速度并对齐中心线切向，清空滑移与 PID 积分，并在 HUD 渲染红色醒目提示横幅，仿真流永不断。
- **自动驾驶控制算法升级**：
  - `CircuitPath` 速度规划升级为全收敛动态遍历（前瞻制动距离由 6 米扩展至 150~200 米，彻底杜绝高速入弯推头）。
  - `UniversalAutoPilot` 升级为「曲率阿克曼几何前馈 + 速度自适应 Stanley 转向 + G-G 摩擦圆纵向协调控制器」，转向角速率平滑限幅（120°/s），打方向时动态限制制动力度，杜绝前轮抱死失控。
- **非交叉上海赛道拓扑重构**：
  - 重构 21 个标准控制点，经严格几何对撞遍历验证**自交叉数由 1 降为 0**，完整保留上赛标志性的 T1-T3 螺线收紧弯、T4-T5 出弯加速区、长后直道、T14 极限制动发卡弯与 T16 冲线弯。

**验证**：`pytest 102 passed`（16.6s）+ `node test_dom.js` 69 项全过。

### R-0830-8（第六波，历史原生直连双叉臂独立悬架架构 Direct Coilover 重构）
- **悬架架构与机械设计彻底脱离方程式推拉杆变体**：
  - 从 `double-wishbone-suspension.html` 完美复刻三套原生独立双叉臂悬架几何架构（`arch: "direct"`）：
    1. `RACE 赛道低趴型` ── `gt3`（🏁 FIA GT3 赛道型：$-3.20^\circ$ 大外倾、低趴硬弹簧、外置直立直连减振柱）；
    2. `SPORT 街道运动型` ── `gt3_sport`（🚗 FIA GT3 日常型：$-0.75^\circ$ 中性外倾、街道平顺阻尼、外置直立直连减振柱）；
    3. `SUV 长行程越野型` ── `baja`（🏜️ SAE Baja 越野型：$R=396\text{mm}$ 全地形越野大轮胎、$[-90, 100]\text{mm}$ 超长轮跳行程、外置大行程越野直立减振柱）。
- **多体求解器与 3D 装配自适应渲染**：
  - `buildMech` 支持 `arch: "direct"`：消除摇臂与推拉杆约束网格，将弹性减振柱直接从下摆臂 `SPR_L (STRUT_OUT)` 连至车身塔顶 `DMP_T (DMP_BODY)`；
  - `addAxleAssemblyPRO` 3D 视图：GT3 与 Baja 下彻底消除车内横置弹簧与摇臂，直接在上下叉臂间渲染粗壮直立螺旋弹簧一体减振柱（含弹簧托盘与活塞筒身）；
  - 左侧 3D 硬点表自适应隐藏无关的摇臂轴硬点行，顶栏/状态栏指示为 `直连 DIRECT`。

**验证**：`pytest 102 passed`（16.8s）+ `node test_dom.js` 71 项全过 + 多车型收敛残差 $0.000\times 10^{-13}$。

## 五、遗留登记（有意保留或后续）

- **F-48**（AbortController 四处复制）：P3 重构，记录不修（双端功能一致，风险>收益）。
- **F-50**（报告/舞台魔法数）：P3，见 EVAL_BENCHMARKS 与 TRK.mu 注释；不引入集中配置（讲义"诚实标注"替代）。
- **F-06**（簧下质量两套口径 unsprungMass vs mU）：P3，文档登记（动力学节点质量 vs 准静态输入为两类参数）。
- **F-07**（arbGeom clamp 无告警）：P3，ok 字段已暴露，UI 告警待 G4 讲义轮。
- **F-29**（max_slip 统计窗口不一致）：P3，见 transient.py 注释。
- **F-56 余项**（window._tireSpinAngles 全局、注释连抄三遍）：无害，已在源码标注。
- **F-20 余项**（HUD accz 物理含义）：已冻结暂停（F-20 主项已修），HUD 阅读项标注 APPROXIMATE。
- **K-4**（负行程残差 0.19mm）：P1 求解器议程，convention.py 已登记，不动。
- **OpenItem-B**（MR 双分支 0.47 vs 0.75）：已进故意不同清单，等待 G2 后续独立议程。
- `test_hand_benchmark.py` 文档漂移：convention.py K-4 注释引用不存在文件，待修（P3）。