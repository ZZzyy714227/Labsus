# 0007 — 全链手册：记忆主干与连接地图（2026-08-30）

## 交付

`learning/全链手册.md`（单文件 Markdown 源）+ `全链手册.html`（MathJax SVG 内联版）+ `全链手册.pdf`（10 页）。
转换工具：`learning/tools/md2pdf_html.js`（Markdown→HTML，mathjax-full 服务端渲染 SVG → headless Edge 打印 PDF）。

## 定位（grill-me 八问定稿）

- **不是浓缩讲义**：是记忆主干 + 连接地图。11 讲 = 肌肉（逐行推导），手册 = 骨架（为什么走到这、下一步去哪、和别处怎么连）。
- 三层金字塔（地基层→机构层→系统层）+ **单主线 12 站**贯穿；系统层含时域分支走廊。
- 每模块卡片 = 五要素：身份 / 核心公式 / 设计思想 / 数字锚点 / 连接（+面试一击）。字数不卡，简洁优雅为准。
- 一头（第 0 章定位宣言：为什么做/可信度/能力边界/前后接口）一尾（第 5 章模型假设 L0-L4·"破在哪"版）。
- 面试视角从四个底层问题出发（为什么做/可信度/边界/接口 + 模型假设），**不被 100 题库带偏**。

## 内容骨架

| 章 | 内容 | 站 |
|---|---|---|
| 0 | 定位宣言（3 价值/三层验证+诚实缺口/适合·不适合//api/v3 数据流） | — |
| 1 | 地基层：坐标系量纲 / 向量旋转刚体 / 数值方法 | 1,2,4 |
| 2 | 机构层：约束 / 求解器 / 定位角瞬心 / 摇臂MR·ARB / 力链衬套 | 3,4,5,6,7 |
| 3 | 系统层：轮胎MF / 载荷转移 / TLLTD / 时域分支 / 验证金字塔 | 8,9,10,11,12 |
| 4 | 单主线全景（一条链走到底 + 三层记忆口诀 + 时域分支说明） | 全 |
| 5 | 模型假设 L0-L4（假设→买到→破在哪→升级路径） | — |
| 附录 | 全链数字锚点总表（27 个 golden 数字 + 手算出处） | — |

## 验证

- 数字与引擎实测交叉校验：残差 18 条、rc_h@0=55.258、golden scrub 54.578/trail 24.643、
  μ=2.286、2.18/13.7Hz、mr→kw 61.9 —— 全部逐位吻合。
- PDF 10 页文本层完整（13 个关键词都在、11858 字符）。
- 公式 36 个全部 SVG 内联渲染成功（零渲染失败标记）。

## 遗留/说明

- 转 PDF 命令依赖环境：`npm install --no-save --prefix %TEMP%/md2pdf mathjax-full@3`（每次转需重装，见工具头注释）。
- HTML 为中间产物；仓库主产物是 .md 与 .pdf。
- 手册内容与十一讲 G4 勘误轮一致（同一数字源）；后续代码改动需同步更新手册与讲义两侧。