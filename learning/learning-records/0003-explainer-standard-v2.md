# 0003 — 讲义制作标准 v2 生效（用户驱动）

日期：2026-08-22

## 事件

用户将第 2 讲（我按 v1 标准制作：ASCII 公式 + 无解析检查点）交给 Gemini 优化，产出的 `第二讲.html` 被用户确认为"比较适合我学习"，指令：以后按此制作、更新制作标准。

## 用户学习偏好（从优化版反推，已固化）

- **MathJax 真公式渲染**（不是 ASCII 伪公式）
- **语义盒分工**：💡直觉（紫）/📐推导（绿）/⚠️警示（黄）/🎯算法架构（蓝）
- **Step 卡片化多步推导**（"保姆级五步"）
- **检查点自带解析**——自学模式，读完即自测，不依赖等批改（教学流程据此调整：检查点答案直接给，口试仍作为章节验收）
- 直觉先行（先物理图景后形式化）、对比表格、语法高亮代码

## 落地

- `learning/explainers/_template.html`（从第二讲提取 head+CSS+骨架，playwright 验证 MathJax 渲染 ✓ 零错误 ✓）
- `learning/EXPLAINER-STANDARD.md`（结构/内容/流程规范全文）
- 记忆 `explainer-standard-v2`；后续章节从第 3 讲起按新标准制作

## 遗留

~~第 1 讲为 v1 格式（含计算投影图）；可选升级，未排期。~~
→ 已于同日升级：`第一讲.html`（v2 标准，87 个 MathJax 公式、三张计算投影图保留嵌入、检查点补解析、新增"镜像 = 反射不是旋转"数学点——用第 2 章 det 判别法解释 steer_axis 反号）。旧版 `ch01-*.html` 留档。
