# LABSUS (FSAE 悬架几何分析与可视化工具) 技术架构与设计解析

本文档旨在帮助开发者全面、彻底地掌握 LABSUS 项目的核心技术细节、架构决策及业务拆分逻辑。LABSUS 是为 Formula Student (FSAE) 赛车设计的悬架几何分析与可视化工具，实现了全向 K&C (Kinematics and Compliance) 扫掠以及实时的姿态求解。

## 1. 项目背景与需求 (Why was this built?)

在 FSAE 赛车的设计与调校过程中，悬架的运动学和动力学特性直接决定了赛车在赛道上的操纵稳定性与抓地力。
传统的商业悬架分析软件（如 Adams/Car, OptimumK）通常存在以下痛点：
*   **黑盒属性**：求解算法不透明，作为学生车队很难理解底层数值收敛逻辑。
*   **计算笨重**：配置复杂，对于初级调校和几何前期的快速迭代（如改变硬点看 Camber 变化）效率较低。
*   **平台限制**：受限于加密狗、特定操作系统甚至校园网环境。

因此，**LABSUS** 的设计初衷是：
*   构建一个**轻量、快速、高度定制化**的白盒悬架求解器。
*   实现**硬点参数的 3D 实时联动可视化**，做到“拖拽即可见”。
*   不仅是设计工具，也是深入理解车辆动力学数值求解的**教育验证平台**。

## 2. 核心技术选型决策 (Why these technologies?)

项目采用了经典的 **前后端分离架构（B/S 架构）**，这也是为了最大化图形渲染与物理方程求解各自的优势。

### 为什么用 Python (FastAPI + NumPy/SciPy) 做计算引擎？
*   **科学计算生态无敌**：悬架求解本质上是求解复杂的多体运动学非线性方程组和空间几何投影。Python 拥有 `scipy.optimize.least_squares` (TRF 算法等)，能极其方便地处理多自由度的非线性求根问题。
*   **快速工程验证**：比起用 C++ 徒手搭建数值求解器，Python 允许开发者将 90% 的时间聚焦在车辆物理建模和几何拓扑上，而不是内存分配。
*   **轻量高性能路由 (FastAPI)**：相比 Django 甚至 Flask，FastAPI 原生支持异步，且基于 Pydantic 提供了开箱即用的类型校验与 Swagger 接口文档，极大地降低了前后端对接的数据扯皮成本。

### 为什么用 Web (HTML5/Canvas + Three.js) 做前端？
*   **3D 交互成熟**：原本计划使用 `PySide6` 开发桌面端软件（历史遗留规划），但 Python 的 3D 图形交互 (例如通过 OpenGL 绑定) 对于构建“拖拽硬点、转动视角”极为困难。而 Web 端有着 `Three.js` (以及原生的 `OrbitControls`, `DragControls`) 这一成熟且性能极高的 3D 库，能极速搭出工程级的可视化环境。
*   **零依赖 / 轻量级部署**：通过 `dwb-pro-allinone.html` 可以实现双击即用的纯本地物理计算模式（内置了基于 JS 的简易 TPHYS 引擎），这极大地方便了车队在无网的赛车场边快速调试。而通过 `dwb-pro-fullchassis.html` 可随时对接后端的满血 Python 接口。

## 3. 业务模块与解耦设计 (Business Domain & Architecture)

系统的核心被切分为三大子系统，体现了良好的多体系统建模逻辑。

### 3.1 核心数学建模 (The Domain Model)
系统采用三维笛卡尔坐标系 (X 前、Y 右、Z 上，原点为前轴/或整车中心投影与地面的交点)。
**双叉臂 (Double Wishbone)** 模型被抽象成几个基本拓扑约束：
1.  **控制臂 (A-arms)**：由两个内侧车架铰点 (Chassis Mounts) 确定一根旋转轴线。外球铰 (Upright Mount) 只能绕该定轴作圆周运动。
2.  **转向拉杆 (Tie Rod)**：其长度是一个硬性距离约束。
3.  **推杆/减震器 (Pushrod/Damper)**：在此架构中作了轻量化处理（推杆上端固接车架），将长度作为从动输出。
4.  **立柱 (Upright)**：被视为一个绝对的刚体。主销 (Kingpin) 轴线由上下A臂外球铰点连接而成，立柱绕主销旋转响应转向动作。

### 3.2 服务架构设计 (Architecture)

#### （后）计算引擎 (LABSUS/engine/src)
*   **`solver.mechanism`** (运动学核心)：由 `pose.py` (姿态求解，主要依托 SciPy 最小二乘求解拉杆长度和转向角的残差), `project.py` (几何投影计算，求取各种倾角) 和 `solver.py` 等构成。这部分专注于输入点云集，输出当前空间位姿。
*   **`src.api`** (应用服务层)：对 FastAPI 引擎提供路由服务。
    *   `v3models.py`：Pydantic 模型，严格规定了悬架两轮 (前/后) 或四轮的数据结构。
    *   `v3service.py` 和 `chassis.py`：业务装配。负责将 API 收到的 JSON 请求拆解，分发给 Mechanism 求解器，再将求解结果打包为定位角度 (Camber, Toe, Caster, KPI 等) 响应返回。

#### （前）渲染客户端 (LABSUS/web)
*   分为两层，单文件组织，没有使用 React/Vue 这一类重型框架，保证了最大的灵活性和最低的上手门槛。
*   利用 `Three.js` 进行线框和小球体的创建 (LineBasicMaterial, MeshLambertMaterial等)。
*   实时监听 HTML Slider 的输入值，通过 `fetch` 生成 JSON 发送 POST 请求。
*   接收结果后更新 Three.js 中缓冲几何体的位置坐标顶点，实现 60FPS 的重绘联动。

## 4. API 接口对接协议 (API Design)

后端的 API 建立在 `/api/v3` 路由之上，严格采取 Pydantic + RESTful 风格，主要入口位于 `server.py`：

### 核心接口梳理
1.  **单轴姿态求解：`POST /api/v3/solve/pose`**
    *   *输入*：包括悬架左右边界的硬点结构(`hardpoints`) 以及驱动命令列表 (`commands`，例如包含轮跳位移 Heave=20mm，转向齿条位移 Rack=5mm)。
    *   *逻辑*：由 `v3service.py -> solve_pose_v3` 处理。
    *   *输出*：左右悬架最终所有节点的空间绝对坐标集，以及各主销的角度（Camber, Toe, Caster, KPI）。
2.  **整车姿态求解：`POST /api/v3/chassis/solve`**
    *   *输入*：整车模型（前轴 + 后轴）的复合参数。支持引入俯仰(Pitch) 和侧倾(Roll) 的求解。
    *   *输出*：四轮的重分布坐标与定位参数。
3.  **K&C 扫掠验证：`POST /api/v3/kandc/{case}`**
    *   *路由变量*：`case` ∈ `[heave, roll, steer]`，自动基于当前硬点配置执行区间扫描（例如轮跳 -30mm 到 30mm 步长 2mm 计算）。
    *   *输出*：一个数组形式的历史轨迹，供前端绘制 Camber vs Heave 等工程特性随动曲线。

### 数据流式传输与状态防抖
前端在连续拖拽滑动条时会产生极高的并发请求。为了防止 Python 服务器被大量的微小异步耗时压垮，在通信层面上设计了关键约定：
*   **无状态 (Stateless)**：引擎不记忆上一帧的求解状态，每发一次请求必须携带“完整的”当前硬点坐标（即 `base` 基准姿态）。这让服务横向扩展和防篡改极其容易。
*   **单点防抖 (Debounce)**：部分高频接口可能在前端或者路由层附加了极低阈值的节流逻辑。

## 5. 没有采用 XXX 方案的取舍考量 (Trade-offs)

*   **为什么不用 C++ / Eigen 进行底层开发并包装 Python 扩展？**
    *   虽然 C++ 对于大矩阵代数和实时渲染是标准做法。但由于 FSAE 团队中懂 C++ 与并行优化的工程师极度断层，Python 的 SciPy 已能够做到毫秒级收敛，在未达到数以千计自由度的柔性体动力学之前，开发生态的易操作性远大于那挤出来的 5 毫秒优化。
*   **为什么只做 1-DOF 简化求解，推杆没连减震器？**
    *   这完全契合**“先做最小版本可行性验证”**的敏捷思想。早期的最大挑战不是非要算准 6DOF 下每个连杆的柔性变形，而是先确保一个理想化的空间四连杆机构“不穿模，防反关节”。让用户先把肉眼能验证的悬架点位定下来，再往里加弹性形变阻力矩阵和避震摇臂非线性比(Motion Ratio)等高级功能。

## 6. 在哪里还能继续深耕（技术延展视野）

如果你要在推免面试中展现宏大的技术架构观，可以向导师描述未来的演进路径：
1.  **引入全向轮胎力学模型 (Pacejka Magic Formula)**：现阶段是以几何运动学(Kinematics)为主导。如果加入 `tire_mf.py` 并计算出接地点六分力反馈给悬架(Compliance)，就能变成一个真正的 K&C 动力学求解器。
2.  **柔性体与衬套 (Bushing Model)**：赛车硬点大多使用球铰（鱼眼），但也存在受弯矩变形的情况。未来可以通过雅可比矩阵计算构件应力梯度。
3.  **参数优化算法**：不再是“手动微调看结果”，而是“给出想要的 Camber 变化曲线（例如目标函数为最小化侧倾外倾畸变），反向采用遗传算法或粒子群算法求出最优硬点位置集”。

---

**总结**：LABSUS 是一个完美兼备**计算力学内功 (SciPy 非线性迭代)** 和 **现代软件工程皮囊 (RESTful + 3D 可视化)** 的本科生全栈工业软件项目。掌握上述所有底层设计，你就可以应对导师从算法收敛到软件架构层面的全方位提问。