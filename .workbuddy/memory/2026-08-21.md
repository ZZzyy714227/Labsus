# 2026-08-21

## 配色改造：Mercedes-AMG Petronas × Hamilton 主题
- 目标：将 FSAE 悬架工作台前端（web/）由 Neo-Brutalist 彩虹配色改为奔驰银箭/汉密尔顿紫主题。
- 保留 brutalist 结构（粗黑边/硬投影/无圆角），仅替换调色板。
- 主色 PETRONAS Teal `#00A19B`；辅助色 Hamilton Purple `#750BB4`；银箭中性 `#E8EAED/#C5C9CE/#8A9099`；碳黑 `#0B0D0E`。
- 语义状态色（绿/琥珀/红）刻意保留以保证工程可读性。
- 改动文件：web/style.css（整体重写变量+类名 teal/purple/silver/dark）、web/index.html（btn-cyan→teal、btn-orange→purple）、web/js/panels.js（3 处按钮类）、web/js/dashboard.js（4 维 color 数据）、web/js/scene3d.js（视口背景对齐银色）。
- 交付：color-scheme-preview.html（配色规格+模块映射+页面预览）。
- 注：modeler.html 为独立内联暗色主题（已偏青/银/紫），double-wishbone-suspension.html 为独立仿真台，本次未改。

## 更正 + 二次改造（上午）
- **目标更正**：用户实际运行的页面是 `web/modeler.html`（start_modeler.bat → run.py → src/main.py 将静态根指向 web/，`/` 重定向到 /modeler.html；index.html 是旧版，仍可经 /index.html 访问）。第一轮改的是旧版 index.html+style.css，用户认可但指明真正要改的是 modeler.html。
- **modeler.html 车队主题改造**：:root 主点缀 #2fa9d6→PETRONAS 青 #00A19B（--acc）、--accd 深青、新增 --purple #9D4EDD、--cy2 #00D2BE；品牌字改汉密尔顿紫；3D CC 配色对象（knu/tie/pathC→#00D2BE，arb→#9D4EDD，ic→#750BB4，axis/gnd 深青）与图例同步，保留金属灰部件色。
- **暗色粗野主义（用户拍板）**：碳黑底 #0B0D0E + 粗银边（--bd3 #8A9099，2-3px）+ 银色硬投影（--bs，box-shadow 硬偏移）+ 分区标题 4px 青左边条粗体 + 按钮 2px 银边/3px 硬投影/hover 位移/on 青实心 + 顶栏 3px 青底条 + 状态栏 3px 银上边。花括号 515/515 平衡校验通过。
- 预览 color-scheme-preview.html 已重写为暗色粗野主义规格（模块映射针对 modeler.html + 暗色 mock）。

## 微调（上午·用户反馈）
- **曲线画布放大**：canvas.plot height 84px→150px、canvas.plot.ks 62px→96px、axChart 内联 60px→110px；canvas.border 由 1px 暗灰升级为 2px PETRONAS 青实心边框（与车队涂装一致）。
- **「丑的灰色」→ PETRONAS 青**：将 :root `--bd3` 与 `--bs` 由 `#8A9099` 改为 `#00A19B`。一改动全链路联动：侧栏/按钮/输入框/分区标题/视口/状态栏的 2-3px 边框 + 全部 box-shadow 硬投影一律转青，整套粗野主义框架直接成为车队涂装色，`#8a9099` 在 modeler.html 与预览中均归零。
- 预览文档同步：子标题「粗银边/银色硬投影」→「粗青边/青色硬投影」；模块映射 7 行（顶栏/侧栏/按钮/输入/视口/状态栏/3D）银→青；银色 swatch 替换为青 swatch。

## 再微调（上午·用户反馈2）
- **青色线条 → 黑色边框**：用户明确「不要青色线条，改用黑色边框线条」。:root `--bd3` 与 `--bs` 由 `#00A19B` 改回 `#000000`；顶栏 3px 底条、.sh 4px 左边条、canvas 2px 框、button:hover 边框一律 `var(--bd3)`（黑）。青色仅保留为功能点缀：button.on 实心填充、input:focus、滑块 hover、文字强调。
- **曲线显示面积再 +50%**：canvas.plot 150→225px、canvas.plot.ks 96→144px、axChart 110→165px（高度 ×1.5）。
- 校验：modeler.html 花括号 515/515；预览文档/预览图（mock）同步转黑；`#00A19B` 只留作 accent 填充。
